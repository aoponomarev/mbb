# Git ignore для защиты папок do-overs и .cursor
# Папка do-overs не должна отслеживаться Git для предотвращения случайных коммитов
# Папка .cursor содержит debug логи и не должна отслеживаться

# Защита папки do-overs от записи через Git
# ВАЖНО: Это предотвращает случайное добавление файлов из do-overs в Git
do-overs/
# Папка .cursor с debug логами
.cursor/
# drafts/ - НЕ игнорируется, чтобы Git отслеживал файлы для коммитов

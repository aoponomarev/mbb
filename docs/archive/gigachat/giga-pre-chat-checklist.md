# Чеклист: Проверка настроек прокси перед использованием чата

> Перед началом использования `giga-chat.html` необходимо убедиться, что все настройки прокси корректны.

## ✅ Шаг 1: Проверить, что прокси создан и активен

### Для Cloudflare Workers:

1. Откройте Cloudflare Dashboard: https://dash.cloudflare.com
2. Перейдите в раздел **Workers & Pages**
3. Найдите Worker `gigachat-proxy` (или другое имя)
4. Убедитесь, что статус: **"Active"** (активен)

**Если Worker не найден или не активен:**
- Worker уже создан, просто убедитесь, что последняя версия активна
- Если Worker отсутствует — смотрите `giga-oauth-proxy-steps-guide.md`

### Для Yandex Cloud Functions:

1. Откройте Yandex Cloud Console: https://console.yandex.cloud
2. Перейдите в раздел **Cloud Functions** (или **Serverless** → **Functions**)
3. Найдите функцию `mbb-gigachat-proxy` (или другое имя)
4. Убедитесь, что статус функции: **"Активна"** (зеленая метка)

**Если функция не найдена или не активна:**
- Функция уже создана, просто убедитесь, что последняя версия активна
- Если функция отсутствует — смотрите `giga-oauth-proxy-steps-guide.md`

## ✅ Шаг 2: Проверить публичный доступ

### Для Cloudflare Workers:

1. На странице Worker убедитесь, что Route настроен (или используйте стандартный URL Worker)
2. Worker должен быть доступен по HTTPS

### Для Yandex Cloud Functions:

1. На странице функции найдите переключатель **"Публичная функция"** (Public function)
2. Переключатель должен быть **ВКЛЮЧЕН** (зеленый/активный)
3. Если выключен — включите его и сохраните изменения

**Важно:** Без публичного доступа прокси недоступен из браузера!

## ✅ Шаг 3: Проверить код прокси

1. Откройте код прокси в редакторе
2. Проверьте, что код соответствует версии из `giga-oauth-proxy-code.md`
3. Особенно важно проверить:
   - ✅ Обработка OPTIONS запросов есть (статус 204)
   - ✅ CORS заголовки присутствуют во всех ответах
   - ✅ Добавлен `Access-Control-Max-Age: 86400`
   - ✅ Функция `getAccessToken()` реализована
   - ✅ Кэширование токенов работает

4. Если код отличается — обновите его из `giga-oauth-proxy-code.md`
5. После обновления кода — задеплойте новую версию

## ✅ Шаг 4: Проверить переменные окружения

### Для Cloudflare Workers:

1. В настройках Worker найдите раздел **"Settings"** → **"Variables"**
2. Убедитесь, что есть переменная:
   - **Variable name:** `GIGACHAT_AUTH_KEY`
   - **Value:** `******` (замаскировано, но должно быть заполнено)

**Если переменной нет или она пустая:**
1. Нажмите **"Add variable"**
2. **Variable name:** `GIGACHAT_AUTH_KEY`
3. **Value:** вставьте ваш Authorization Key (Base64) из `giga-get-api-key.md`
4. Нажмите **"Save"**

### Для Yandex Cloud Functions:

1. На странице функции найдите раздел **"Переменные окружения"** (Environment Variables)
2. Убедитесь, что есть переменная:
   - **Ключ:** `GIGACHAT_AUTH_KEY`
   - **Значение:** `******` (замаскировано, но должно быть заполнено)

**Если переменной нет или она пустая:**
1. Нажмите **"Добавить"** (или **"Добавить переменную"**)
2. **Ключ:** `GIGACHAT_AUTH_KEY`
3. **Значение:** вставьте ваш Authorization Key (Base64) из `giga-get-api-key.md`
4. Нажмите **"Сохранить"**

**Как получить Authorization Key:**
- См. инструкцию: `giga-get-api-key.md`
- Или перейдите: https://developers.sber.ru → GigaChat API → Настройки → Ключи доступа

## ✅ Шаг 5: Проверить настройки выполнения

### Для Cloudflare Workers:

1. Проверьте, что Worker использует достаточно ресурсов для обработки запросов
2. Убедитесь, что таймаут установлен (обычно 30 секунд или больше для Worker)

### Для Yandex Cloud Functions:

1. На странице функции найдите раздел **"Последняя версия"** (Latest version)
2. Проверьте следующие параметры:

   - **Среда выполнения:** `nodejs18`, `nodejs20` или `nodejs22` (поддерживается Node.js 18+)
   - **Таймаут:** `30 секунд` (рекомендуется 30 сек или больше для OAuth + API запросов)
   - **Память:** `128 МБ` (минимум, можно больше при необходимости)
   - **Точка входа:** `index.handler` (для Yandex Cloud Functions)

**Если таймаут меньше 30 секунд:**
- Рекомендуется увеличить до 30 секунд для OAuth + API запросов
- Можно изменить при создании новой версии

## ✅ Шаг 6: Проверить URL прокси

### Для Cloudflare Workers:

1. На странице Worker найдите раздел **"Triggers"** или **"Overview"**
2. Должен быть URL вида:
   ```
   https://gigachat-proxy.your-subdomain.workers.dev
   ```
   или
   ```
   https://yourdomain.com/gigachat
   ```
   (если настроен Route)
3. **Скопируйте этот URL** — он понадобится для настройки чата

### Для Yandex Cloud Functions:

1. На странице функции найдите раздел **"Описание"**
2. Найдите поле **"Ссылка для вызова"** (Call link)
3. Должен быть URL вида:
   ```
   https://functions.yandexcloud.net/d4erd8d1pttbufsl26s1
   ```
4. **Скопируйте этот URL** — он понадобится для настройки чата

## ✅ Шаг 7: Протестировать прокси (опционально, но рекомендуется)

1. Откройте `giga-cors.html` в браузере
2. Введите URL прокси (из шага 6)
3. Поле **"Authorization Key"** можно оставить пустым (если он в переменных окружения)
4. Выберите модель (например, "GigaChat")
5. Введите тестовый запрос: "Привет! Расскажи о себе."
6. Нажмите **"Отправить запрос"**
7. Убедитесь, что получен успешный ответ (статус 200, есть текст ответа)

**Что проверить в логах:**
- [ ] OAuth токен получен успешно
- [ ] Запрос к Chat API отправлен успешно
- [ ] CORS заголовки присутствуют в ответе
- [ ] Ответ от API корректный
- [ ] Нет ошибок CORS в консоли браузера

**Если тест не проходит:**
- Проверьте логи прокси (раздел "Logs")
- Убедитесь, что все настройки выше выполнены
- См. `giga-cors-troubleshooting.md` (после создания) для устранения проблем

## 📋 Итоговый чеклист

Перед использованием `giga-chat.html` убедитесь, что выполнено:

- [ ] Прокси создан и активен
- [ ] Публичный доступ включен
- [ ] Код прокси обновлен (из `giga-oauth-proxy-code.md`)
- [ ] Переменная окружения `GIGACHAT_AUTH_KEY` настроена
- [ ] Таймаут установлен (рекомендуется 30 секунд)
- [ ] URL прокси скопирован для использования в чате
- [ ] (Опционально) Прокси протестирован через `giga-cors.html`

## 🚀 После выполнения всех проверок

1. Откройте `giga-chat.html` в браузере
2. В боковой панели введите:
   - **URL прокси:** URL из шага 6
   - **Authorization Key:** можно оставить пустым (если хранится в env прокси)
   - **Модель:** выберите нужную (GigaChat, GigaChat-Pro, GigaChat-Max)
3. Нажмите "Сохранить" для URL прокси и Authorization Key (если указан)
4. Начните чат — отправьте первое сообщение

## 📚 ССЫЛКИ

- Код прокси: `giga-oauth-proxy-code.md`
- Пошаговое руководство: `giga-oauth-proxy-steps-guide.md`
- Получение Authorization Key: `giga-get-api-key.md`
- Устранение проблем: `giga-cors-troubleshooting.md` (после создания)
- План интеграции: `giga-integration-mbb-plan.md`

# Документация утилиты подсчета статистики проекта

> Оглавление `docs/doc-stats-counter.md`
- § Назначение и принципы работы
- § Использование
- § Формат результатов
- § Кэширование
- § Интеграция в протокол начала дня

---

## § НАЗНАЧЕНИЕ И ПРИНЦИПЫ РАБОТЫ

### Цель

Утилита `scripts/stats-counter-node.js` предназначена для подсчета статистики проекта:
- Количество строк кода (исключая комментарии и пустые строки)
- Количество строк комментариев (шапки файлов и inline комментарии)
- Количество файлов по категориям
- Общее количество строк в файлах

### Принципы подсчета

1. **Разделение кода и комментариев:**
   - Строки кода: непустые строки, не являющиеся комментариями
   - Строки комментариев: однострочные и многострочные комментарии
   - Шапки файлов: блоки комментариев в начале файла до первой строки кода

2. **Учет игнорируемых папок:**
   - `node_modules/`, `.git/`, `do-overs/`, `drafts/`
   - Файлы из `.gitignore` и `.cursorignore`

3. **Категории файлов:**
   - **JS**: файлы `.js` (исключая `*template.js`), за вычетом шапок комментариев
   - **HTML**: файлы `.html` и `*template.js`, за вычетом шапок комментариев
   - **CSS**: файлы `.css` в папке `styles/`, за вычетом шапок комментариев
   - **DOCS**: файлы `.md` (исключая `docs/logs/` и `docs/archive/`) плюс строки шапок комментариев из всех файлов с кодом

---

## § ИСПОЛЬЗОВАНИЕ

### Базовое использование (с визуализацией)

```bash
node scripts/stats-counter-node.js
```

Выводит красиво отформатированную статистику с визуализацией (диаграммы из Unicode-блоков, цвета через ANSI коды).

### Простой текстовый формат

```bash
node scripts/stats-counter-node.js --simple
```

Выводит статистику в компактном текстовом формате без визуализации.

### JSON формат

```bash
node scripts/stats-counter-node.js --json
```

Выводит JSON объект со статистикой для программной обработки.

### Принудительное обновление кэша

```bash
node scripts/stats-counter-node.js --force-refresh
```

Пересчитывает статистику без использования кэша (можно комбинировать с `--simple` или `--json`).

### Интеграция в код

```javascript
const StatsCounter = require('./scripts/stats-counter-node.js');
const counter = new StatsCounter();

const stats = await counter.getStats();
console.log(counter.formatStats(stats));
```

---

## § ФОРМАТ РЕЗУЛЬТАТОВ

### JSON структура

```json
{
  "js": {
    "lines": 9952,
    "comments": 4867,
    "files": 67,
    "totalLines": 16585
  },
  "html": {
    "lines": 5810,
    "comments": 402,
    "files": 20,
    "totalLines": 6212
  },
  "css": {
    "lines": 378,
    "comments": 237,
    "files": 6,
    "totalLines": 615
  },
  "docs": {
    "lines": 11367,
    "comments": 2740,
    "files": 54,
    "totalLines": 11367
  },
  "timestamp": 1767518022549
}
```

### Поля статистики

- **lines**: количество строк кода (исключая комментарии и пустые строки)
- **comments**: количество строк комментариев (шапки файлов и inline комментарии)
- **files**: количество файлов в категории
- **totalLines**: общее количество строк в файлах (включая пустые строки)
- **timestamp**: время последнего подсчета (Unix timestamp в миллисекундах)

### Форматированный вывод с визуализацией

Метод `formatStats()` возвращает красиво отформатированную строку с визуализацией:

- **Компактная таблица**: Все данные в структурированной таблице с легендой (цветные символы категорий)
- **Тепловая карта**: Единый массив Unicode-блоков (█ ▓ ▒ ░) показывает пропорциональное распределение строк кода
- **Цвета**: ANSI коды для цветного вывода в терминале (если поддерживается)
- **Проценты**: Доля каждой категории от общего объема кода

Пример вывода:
```
📊 Статистика проекта

┌─────────┬──────────────┬──────────────┬──────────┬──────────┐
│ Категория │ Строк кода   │ Комментариев │ Файлов   │   Доля   │
├─────────┼──────────────┼──────────────┼──────────┼──────────┤
│ █ JS     │       10,006 │        4,930 │       67 │   50.2% │
│ ▓ HTML   │        4,960 │          402 │       20 │   24.9% │
│ ▒ CSS    │          302 │          237 │        6 │    1.5% │
│ ░ DOCS   │        4,659 │        2,740 │       16 │   23.4% │
├─────────┼──────────────┼──────────────┼──────────┼──────────┤
│ Итого   │       19,927 │        8,309 │      109 │ 100.0% │
└─────────┴──────────────┴──────────────┴──────────┴──────────┘

Распределение кода:
██████████████████████████████▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▒░░░░░░░░░░░░░░
█ JS  ▓ HTML  ▒ CSS  ░ DOCS
```

**Примечание:** Визуализация использует Unicode-символы и ANSI коды цветов. В терминалах без поддержки ANSI цвета будут отключены, но таблица и тепловая карта останутся видимыми.

---

## § КЭШИРОВАНИЕ

### Механизм кэширования

Результаты подсчета сохраняются в `docs/.stats-cache.json` для снижения нагрузки при повторных запросах.

### Обновление кэша

Кэш автоматически обновляется при:
- Принудительном обновлении через `--force-refresh`
- Повреждении файла кэша
- Отсутствии файла кэша

### Игнорирование кэша

Файл `docs/.stats-cache.json` добавлен в `.gitignore` и не отслеживается Git.

---

## § ИНТЕГРАЦИЯ В ПРОТОКОЛ НАЧАЛА ДНЯ

### Использование в протоколе

В протоколе начала дня (раздел "Стартовый отчёт" → "Статистика проекта") утилита вызывается автоматически:

```bash
node scripts/stats-counter-node.js
```

Результат форматируется и выводится в стартовом отчёте.

### Формат вывода в протоколе

```
Статистика проекта (текущее состояние):

JS: N строк кода, M строк комментариев (K файлов) | Всего строк: L
HTML: N строк кода, M строк комментариев (K файлов) | Всего строк: L
CSS: N строк кода, M строк комментариев (K файлов) | Всего строк: L
DOCS: N строк контента, M строк шапок комментариев (K файлов) | Всего строк: L
```

---

## ПРИМЕЧАНИЯ

1. **Производительность:** Кэширование значительно снижает нагрузку при повторных запросах. Первый запуск может занять несколько секунд для больших проектов.

2. **Точность:** Подсчет комментариев может не учитывать все edge cases (например, комментарии внутри строк). Для точного анализа рекомендуется использовать специализированные инструменты.

3. **Расширяемость:** Утилиту можно расширить для подсчета других метрик (сложность кода, количество функций, размер файлов в байтах и т.д.).
